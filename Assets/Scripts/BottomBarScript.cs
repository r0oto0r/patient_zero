using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements;

public class BottomBarScript : MonoBehaviour
{
    public CommandExecutorScript CommandExecutorScript;
    void Start()
    {
        // Button roundFinishButton = GetComponent<UIDocument>().rootVisualElement.Q<Button>("RoundFinishButton");
        // roundFinishButton.clicked += () => CommandExecutorScript.ExecuteCommands();
    }

    void Update()
    {
        
    }
}
